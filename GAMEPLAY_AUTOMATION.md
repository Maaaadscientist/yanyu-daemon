# 烟雨江湖自动化玩法与录制手册

本文按当前仓库和 2026-08-17 的现场实测整理。它描述的是客户端可观察到的控制规律，以及本项目如何安全地把这些规律表示成自动化动作。它不是游戏内部协议，也不假设客户端提供可直接调用的寻路接口。

## 1. 当前结论

- `coordinates.py` 中的 `pos` 是参考截图像素，不是游戏地图坐标。
- `map_pos` 和 OCR 读到的 `(x,y)` 才是游戏地图坐标。
- 普通步行是“点击当前视野中的地面，让客户端自行寻路”；建筑、屋顶、围栏、悬崖和镜头边界都会使一个理论直达点失效。
- 旧路线把等待时间写死在下一动作上。49 条旧路线共有 1325 个动作，其中 45 条都重复实现了马车旅行。
- 大理实测步行段稳定耗时约 `0.54-1.41s`，猪圈最后一格因绕围栏耗时 `3.147s`；旧路线常用的 `2.5-6.5s` 固定等待明显偏保守。
- 牛的 `宰杀` 点击和猪的 `宰杀` 点击会直接启动刷新计时，没有第二次确认。
- 猪的文字中心不是有效热区。应识别 `猪` 所在行，再向左偏移到图标热区点击。
- 旧脚本没有一段可以可靠认定为多段轻功。新增的 `rapid_clicks` 专门表达起跳后的快速连续点击，不能把普通路线中的短延迟猜成轻功。
- 智能调度把资源刷新时间作为目标时刻，提前旅行，并在选择动物前等待到目标时刻；刷新锚点取真实 `宰杀` 点击的派发时间。

## 2. 文件职责

| 文件 | 责任 |
| --- | --- |
| `coordinates.py` | 旧式参考像素、旧路线、地图资源坐标和智能流程草稿 |
| `automation.py` | 查找/激活窗口、参考像素缩放、地图特征对齐、点击、拖动、原子快速连点、截图 |
| `smart_automation.py` | OCR、状态等待、文字热区、流程组合、运行时耗时学习 |
| `control_recorder.py` | 录制鼠标、拖动、OCR 状态、刷新锚点和轻功连点组 |
| `procedure_runner.py` | 独立读取状态、列出、dry-run 或运行一个智能流程 |
| `tracking_click.py` | 7x24 调度、提前出发、精确刷新锚点、失败恢复 |
| `recording_analyzer.py` | 量化录制、校验点、文字定位和快速连点间隔 |
| `procedures/*.json` | 可执行智能流程和可复用公共片段 |

## 3. 两套坐标系

### 3.1 参考截图坐标

旧路线和 JSON 动作中的 `point`、`start`、`end` 使用参考截图坐标。执行时换算为当前窗口位置：

```text
screen_x = window_left + reference_x * current_window_width / reference_image_width
screen_y = window_top  + reference_y * current_window_height / reference_image_height
```

当前 `game_screenshot.png` 是 `2102x1632`。当前窗口在 macOS 中约为 `1105x857` 点，Retina 截图可能是 `2210x1714` 像素。这是正常的逻辑点/物理像素差异。

不要只更新 `game_screenshot.png` 的尺寸而不复核已有点位。参考图尺寸变化后，旧点位仍代表旧图上的像素，至少要先 dry-run 并抽查关键按钮。

### 3.2 游戏地图坐标

右栏标题中的 `大理 (29,3)` 是地图坐标。它用于确认角色实际到达哪里：

```python
map_pos = {
    '大理': {
        '马车落点': (32, 18),
        '牛': (28, 11),
        '猪': (29, 3),
    },
}
```

Vision 偶尔会把 `(29,6)` 识别成 `（29.6）`。解析器现在接受中英文逗号以及句点误识别。

### 3.3 为什么不能直接计算地面像素

一个地图坐标差不能稳定换算成一个屏幕像素差，原因包括：

- 镜头通常跟随角色，但在地图边缘会停止移动；
- 等距地图的两个坐标轴不是屏幕水平/垂直轴；
- 不可通行格会触发绕路，或者让点击完全无效；
- 屋顶和前景可能遮住可行走地面；
- 场景入口、楼梯和悬崖改变可到达区域。

因此本项目使用“实测地面点击 + OCR 目标坐标”的闭环，而不是仅凭终点计算一次点击。

## 4. 从旧脚本抽象出的游戏动作

### 4.1 激活客户端

旧路线通常先点 `烟雨江湖` 标题栏。现在执行器读取窗口所属 PID，通过 AppKit 激活应用，再执行动作。这样不会把第一下业务点击浪费在窗口聚焦上。

### 4.2 清理当前界面

旧脚本大量点击 `空白`，实际承担多个不同目的：

- 关闭对话或结果提示；
- 离开上一次交互层；
- 给界面动画留出时间；
- 在状态不明确时尝试回到地图。

这类点击在智能流程中应逐步替换为明确的 OCR 前置条件或后置条件，不能把所有失败都继续堆叠 `空白`。

### 4.3 马车票旅行

通用玩法顺序是：

1. 打开 `包袱`。
2. 点击 `叫唤马车`。
3. 在世界地图上拖动一到多次，让目的地出现且弹出面板不被边缘裁切。
4. 点击目的地马车图标。
5. 点击 `确定前往` 或旧路线中的 `某地出发`。
6. 等待地图名称和落地坐标稳定。

`打开包袱` 属于可逆 UI 动作：点击后必须识别到 `叫唤马车`，否则最多重试一次。地图旅行、普通移动和刷新动作仍不盲目重试。

拖动必须记录起点、终点和持续时间。它不是角色移动，也不能用点击动作代替。

世界地图会保留/改变平移位置，且打开时与当前城市有关，所以目的地图标不能只保存一个绝对像素。当前实现用 `assets/world_map_reference.jpg` 保存一张成功地图视图，运行时通过 ORB 特征匹配和 RANSAC 仿射估计，把参考图中的大理点映射到当前画面。匹配数、内点比例、缩放或旋转不满足阈值时直接失败，不盲点目的地；点开后还必须在弹窗区域识别到 `大理`，再查找 `确定前往`。

当前所在城市的马车图标会被白色角色标记替换，因此任何马车流程都不能把当前地图再次当成出发目的地。调度器会先 OCR 当前地图；若与马车目的地相同，就只执行洛阳或南阳渡路线的马车前缀，确认落地后再从目标路线第一步重跑。`sleep1` 的世界地图入口叫“乌思雪原”，实际落地页 OCR 为 `逻邪河谷`，两者在恢复判断中按同一地点处理。

### 4.4 普通步行

普通步行动作由三部分组成：

```json
{
  "type": "click",
  "point": [1115, 649],
  "expect": {"map": "大理", "coordinate": [35, 12]},
  "timeout": 10.0
}
```

- `point`：当前镜头下的地面落点；
- `expect`：角色最终必须稳定到达的地图/坐标；
- `timeout`：容纳寻路、卡顿和 OCR 采样的上限，不是固定睡眠。

等待在连续两个 OCR 样本都命中后立即结束。移动超时默认失败，不会自动重放同一个屏幕点，因为镜头变化后重复点击可能指向另一个地图格。

### 4.5 场景切换和入口

旧路线中的 `长白1`、`华山1`、`天山1` 等点既可能是普通地面，也可能跨越地图入口。判断依据应是点击后的地图名称/坐标变化，而不是变量名。跨图动作要使用更长 timeout，并验证新地图。

### 4.6 资源交互

旧动物路线通常是：

```text
到资源附近 -> 边栏1/边栏2 -> 空白 -> 确认 -> 空白
```

这是对旧 UI 的像素猜测。当前智能流程改成：

```text
到目标坐标 -> 精确识别资源文字 -> 点击有效热区
-> 等“宰杀/采集”出现 -> 点击刷新动作
-> 等资源条目消失
```

`exact_text: true` 很重要。否则查找 `猪` 时，`猪圈` 也会被子串匹配。

文字中心和有效热区不一致时使用 `text_click_offset`：

```json
{
  "target_text": "猪",
  "exact_text": true,
  "text_click_offset": [-111, 7]
}
```

### 4.7 保存

旧 `save` 路线点击 `保存`、`保存确认`，最后关闭提示。它属于资源刷新动作之后的清理操作。保存失败不能导致再次宰杀，所以智能任务在刷新锚点之后失败时会标记为 `post_anchor_failed`，仍保留完整刷新周期。

## 5. 大理公共马车与绕障路线

公共片段位于 `procedures/_dali_travel_to_cow_area.json`，牛和猪通过 `include` 复用它。

| 步骤 | 参考动作 | 结果 | 实测稳定耗时 |
| --- | --- | --- | --- |
| 0 | OCR 负向状态检查 | 当前地图不能是大理 | 同城图标被角色标记覆盖时立即停止 |
| 1 | 包袱 `(485,1605)` | 打开道具栏 | UI 动作 |
| 2 | `叫唤马车` `(620,1221)` | 打开世界地图 | OCR 必须命中 |
| 3 | `(1100,1400) -> (1100,849)` | 地图纵向拖动 | `1.0s` 拖动 |
| 4 | `(449,1049) -> (1149,1049)` | 大理面板完整显示 | `1.0s` 拖动 |
| 5 | 对齐参考点 `(1130,1428)` | 动态选择大理图标并验证 `大理` 弹窗 | ORB/RANSAC；不是固定屏幕点 |
| 6 | OCR `确定前往` | 落地 `(32,18)` | `1.692s` |
| 7 | `(1198,819)` | `(33,18)` | `0.538s` |
| 8 | `(1330,733)` | `(35,15)` | `1.232s` |
| 9 | `(1115,649)` | `(35,12)` | `0.980s` |
| 10 | `(1398,609)` | `(35,7)` | `1.260s` |
| 11 | `(574,720)` | `(32,8)` | `1.413s` |
| 12 | `(590,729)` | `(29,8)` | `0.847s` |
| 13 | `(729,899)` | `(28,9)` | `0.767s` |
| 14 | `(689,899)` | `(27,11)` | `0.837s` |
| 15 | `(1179,899)` | `(28,11)` | `0.605s` |

这些点不是最短几何路线，而是绕开城门、屋顶、楼梯和高地边界的实测路线。

## 6. 大理牛 `(28,11)`

`dali_cow` 在公共片段后执行：

1. 到预计刷新时刻后，精确识别 `牛`。
2. 点击 OCR 识别到的牛行。
3. 等待 `宰杀` 按钮出现。
4. 点击 `宰杀`，立刻记录 60 分钟刷新锚点。大理牛属于牛棚圈养动物，不使用牧场的 180 分钟规则。
5. 连续两个 OCR 样本确认 `牛` 条目消失。

现场确认 `宰杀` 本身就是刷新动作，没有第二个确认框。

## 7. 大理猪 `(29,3)`

最初记录中的 `(1090,786)` 被误以为可以从 `(28,11)` 直达。2026-08-17 独立复测时角色保持在 `(28,11)`，证明该直达点无效，现已删除。

当前分支是重新现场验证的六段路径：

| 参考点击 | 起点 | 稳定终点 | 说明 |
| --- | --- | --- | --- |
| `(800,750)` | `(28,11)` | `(26,11)` | 向西避开屋顶 |
| `(1100,800)` | `(26,11)` | `(27,10)` | 转入北向道路 |
| `(1119,760)` | `(27,10)` | `(27,8)` | 实测 `1.168s` |
| `(1300,800)` | `(27,8)` | `(29,6)` | 到猪圈南侧 |
| `(1120,650)` | `(29,6)` | `(28,3)` | 绕过围栏 |
| `(1150,850)` | `(28,3)` | `(29,3)` | 绕行耗时 `3.147s` |

到达后：

1. 精确识别 `猪`，不能匹配 `猪圈`。
2. 从文字中心偏移 `(-111,+7)` 到左侧图标热区。
3. 等待并点击 `宰杀`，记录 60 分钟刷新锚点。
4. 确认 `猪` 消失，但允许 `猪圈` 继续存在。

猪的现场宰杀和重新整理后的六段移动均已成功。2026-08-17 的回归实际暴露并修复了世界地图平移、`确定前往` 弹窗横向移动和负向状态断言二次等待三个问题。修复后的当前 25 动作版本已在一个连续进程中完整通过：从外地图出发、动态对齐大理、走完公共与猪分支、等待计划闸门、宰杀、确认猪消失并保存。

## 8. 轻功与快速多段跳

### 8.1 为什么不能用普通 click 列表

普通动作之间会发生日志输出、OCR、`pyautogui` 默认 pause 和固定等待。多段轻功在起跳后要求连续落点快速完成，这些额外工作可能让角色摔落。

### 8.2 `rapid_clicks` 动作

```json
{
  "type": "rapid_clicks",
  "label": "起跳并完成三段落点",
  "points": [[1000, 820], [1180, 700], [1360, 590]],
  "intervals": [0.18, 0.17],
  "max_gap_seconds": 0.40,
  "expect": {"map": "目标地图", "coordinate": [10, 8]},
  "timeout": 8.0
}
```

执行保证：

- 开始前由普通动作完成窗口激活和起点状态确认；
- 组内关闭 `pyautogui.PAUSE`；
- 所有点击按同一个单调时钟的绝对 deadline 调度，避免逐段 sleep 累积漂移；
- 组内不截图、不 OCR、不写逐点击日志；
- 即使某一段已经超时，也会先把剩余落点全部点完，再报告时序错误，避免中途主动停止导致必然摔落；
- 整组结束后才验证落地坐标；
- 快速组不自动重试。

示例坐标只是格式说明，不能直接运行。必须录制一条实际成功的轻功路线。

### 8.3 录制轻功

启动智能录制：

```bash
python3.12 control_recorder.py \
  --name my_qinggong \
  --smart \
  --map 大理 \
  --start 10,10 \
  --target 15,5 \
  --rapid-max-gap 0.5
```

操作顺序：

1. 普通移动到安全起跳格，并等待坐标稳定。
2. 按 `F6` 标记起点检查。
3. 按 `F5`，等待终端打印 `rapid capture ... armed`。后台 OCR 会先完全停下。
4. 手动点击起跳和每一段落点，不做停顿或截图。
5. 安全落地后再按 `F5` 结束组，OCR 恢复。
6. 等坐标稳定，按 `F6` 标记落地检查。
7. 按 `F9` 保存。

录制器会根据成功操作保存每两个点击的真实间隔。允许最大间隔至少为 `--rapid-max-gap`，也不会小于本次成功录制的最大间隔加 `0.05s`。

只要存在 F5 快速组，生成的旧式 `.py` 文件就是空的保护桩。必须运行 `.procedure.json`，因为旧 tuple runner 无法保证轻功时序。

## 9. 录制普通新流程

例如新增一只刷新 180 分钟的羊：

```bash
python3.12 control_recorder.py \
  --name dali_sheep \
  --smart \
  --install \
  --map 大理 \
  --start 32,18 \
  --target 20,20 \
  --interval-minutes 180 \
  --lead-seconds 75 \
  --before-route sleep1 \
  --after-route save
```

快捷键：

- `F5`：开始/结束原子快速连点组；
- `F6`：要求上一个动作完成后验证稳定坐标；
- `F7`：把上一个动作标为唯一刷新锚点；
- `F8`：暂停/继续录制；
- `F9`：结束并写文件。

普通录制原则：

1. 从可重复的已知状态开始。
2. 移动后等待角色停止，再发下一次移动。
3. 障碍附近多用短 waypoint，不追求一键最短路。
4. 只在真实杀死/采集动作后按 `F7`。
5. 失败尝试也保留在 raw recording 中，但安装前要删除无效点击和人工观察等待。
6. 如果点的是文字左侧图标，录制器会尝试保存相对文字中心的偏移。
7. 配置 `--lead-seconds` 后，录制器会把 F7 锚点前最近的非移动点击自动设为计划闸门；缺少 F7 时流程校验会拒绝安装。

输出：

```text
recordings/<name>_<timestamp>.jsonl
recordings/<name>_<timestamp>.states.jsonl
recordings/<name>_<timestamp>.procedure.json
procedures/<name>.json
```

## 10. 公共流程片段

JSON 支持：

```json
{"type": "include", "procedure": "_dali_travel_to_cow_area"}
```

片段本身也是一个 `procedures/*.json` 文件，但应设置：

```json
{"enabled": false, "library": true}
```

加载时会递归展开片段，未知引用和循环引用会直接报错。公共旅行更新后，只需修改一个文件，所有引用它的资源路线都会生效。

## 11. 延迟模型

旧动作：

```python
(pos['确认'], 1.5)
```

表示在点击 `确认` 之前等待 `1.5s`，不是点击之后等待。

应按目的区分时间：

| 类型 | 处理方式 |
| --- | --- |
| UI 动画 | 很短 `before_delay`，随后等待目标文字 |
| 地图加载 | 等待地图名和落地坐标 |
| 普通步行 | 等待目标坐标稳定 |
| 战斗/长动画 | 等待明确 UI 或给独立 timeout |
| 多段轻功 | 原子绝对 deadline，不插入 OCR |
| 资源刷新 | 调度目标时刻 + 真实点击锚点 |

当前旧路线统计：

- 49 条路线；
- 1325 个动作：1241 次点击、84 次拖动；
- 固定等待合计约 `2901.6s`；
- delay 中位数 `1.0s`，90 分位 `4.5s`；
- `空白` 出现 261 次，确认类动作超过 180 次。

这些数字说明旧脚本适合当作玩法演示数据，但不适合继续用“增加 sleep”作为主要修复方式。

## 12. 精确刷新调度

智能任务状态中的时间含义：

- `next_due`：资源预计重新出现的目标时刻；
- `lead_seconds`：提前开始旅行的秒数；
- `lead_samples`：提前量学习样本数；
- `last_refresh_anchor`：上一次真实刷新点击时间；
- `last_status`：`ok`、`failed` 或 `post_anchor_failed` 等。

流程：

1. 调度器在 `next_due - lead_seconds` 开始任务。
2. 执行马车和步行。
3. 遇到 `wait_until_scheduled: true` 的资源选择动作时，如果提前到达，就等待到 `next_due`。
4. 识别资源、打开交互并点击 `宰杀/采集`。
5. 从该点击时间计算下一次 `next_due`。
6. 扣除提前到达后的空等时间，学习真实“出发到刷新动作”耗时，并保留 `2s` 余量。

通过生产日常入口 `test.py` 或单流程入口 `procedure_runner.py` 独立完成的真实智能流程也会把精确锚点同步到 `scheduler_state.json`，避免手工执行刚宰杀完，守护进程又把它当成新任务。dry-run、失败和无刷新锚点流程不会同步；特殊情况下可用 `--no-sync-scheduler` 禁用。仓库自动测试只使用 `tests/`，不会执行 `test.py`。

如果启动时同时存在大量历史过期任务，优先级依次为：已进入提前出发窗口且目标时刻尚未到的精确任务、已经错过目标的精确任务、旧像素任务。每一类内部再按最早目标排序。这样守护进程离线数小时后恢复时，不会为了清理旧路线积压而继续错过每小时资源。

普通循环每完成一个任务就重新计算优先级，不再一次性锁定整批过期任务。距离下一个精确任务开始窗口不足默认 `120s` 时，不再启动新的过期旧路线；可通过 `--precision-reserve-seconds` 调整。正在执行的单条旧路线不会被中途打断。

牛和猪初始提前量为 `75s`。有真实调度样本后使用 EWMA 更新。

一次隔离的猪刷新计时回归中，流程提前到达并在选择猪前等待 `25.927s`；计划资源时刻为 `06:03:54.566`，实际 `宰杀` 锚点为 `06:03:57.887`。约 `3.32s` 的差值来自刷新后 OCR 定位猪、打开交互和识别 `宰杀`，下一周期仍从实际锚点计算，因此不会把这段处理时间逐轮累积成漂移。

立即人工运行不会等待计划时刻：

```bash
python3.12 tracking_click.py --run-now dali_pig
```

正常 due task 才会应用计划时间门。

只等待并验证下一次计划任务后退出：

```bash
python3.12 tracking_click.py --only-task dali_pig --wait-once
```

`--once` 只检查当前已经到期的任务；`--wait-once` 会等到提前窗口再运行一个 due batch。

### 12.1 安全停止

`tracking_click.py` 默认注册全局 `<ctrl>+c`。即使守护进程运行在后台 `screen` 中，只要按下该组合键，就会设置统一停止事件：普通点击间隔、OCR 轮询、刷新时间门和调度轮询会立即结束，`finally` 会保存完整的 `scheduler_state.json`。

正在执行的 `rapid_clicks` 是例外。停止请求会等本组快速连点全部完成后再生效，避免多段轻功只执行一半而摔落。日志依次包含 `scheduler_hotkey_stop_requested` 和 `scheduler_stopped`，后者的 `reason` 为 `global_hotkey`。

可自定义或禁用全局热键：

```bash
python3.12 tracking_click.py --stop-hotkey '<ctrl>+<shift>+x'
python3.12 tracking_click.py --no-stop-hotkey
```

### 12.2 人工输入暂停与从头恢复

当前 macOS 游戏客户端无法接收可靠的 PID 定向后台点击，因此自动化仍与系统桌面共用鼠标。2026-08-17 在重新授予 Python 辅助功能权限后再次测试：private/HID 两种事件源均能在不移动光标、不抢终端或 ToDesk 前台的情况下投递到游戏 PID，但登录页“账号登录”按钮没有响应；辅助功能树也只有一个通用 `AXTextArea`，没有游戏内部控件。该权限对前台自动化和人工输入检测有用，但不会让 UIKit 游戏画布接受后台点击。调度器继续采用“检测到人工输入就让权”的方式处理：

1. `pynput` 全局监听物理鼠标移动、点击、滚轮和键盘按下。
2. `pyautogui` 的移动、点击和拖动被包在 `automation_input()` 保护区内，并带短暂事件回传宽限期，不会把脚本自己的事件判成人工输入。
3. 人工事件把 `pause_event` 置位；普通动作在下一个动作边界停住，尚未完成的相对 delay 保留剩余时间。
4. 多段轻功 `rapid_clicks` 是原子动作，不在组内暂停。连点完成后立即响应暂停或停止，避免只跳一半。
5. `runtime_control.json` 立即保存任务、路线序号、下一动作序号、动作标签、阶段、暂停原因和时间。
6. 工作线程再用 Vision OCR 补充地图名与坐标。监听线程本身不做截图/OCR，避免阻塞系统输入回调。

恢复不再要求用户复原旧地图和旧坐标，也绝不消费 `next_action_index`。按默认 `Ctrl-Alt-R` 或网页“安全从头恢复”后：

1. 旧断点写入 `automation_checkpoint_abandoned`，只保留作审计。
2. 若已在游戏内，只要求地图与坐标可读，不要求与旧断点一致。
3. 若在登录页，依次识别并执行：`账号登录`、已保存账号的 `登录`、关闭更新公告、勾选协议、`开始游戏`。
4. 检测到“其他设备上传过更新版本存档（或者设备登出过）”时，只点击 `确认` 强制下载服务器存档；恢复过程不输入密码，也不选择本地上传。
5. 等待默认 3 秒安静倒计时；期间有新人工输入就拒绝本次恢复。
6. 生成 `restart_task` 恢复票据，从任务第 1 条路线、第 1 个动作重新运行。
7. 若所有活动资源都已刷新，不锁定旧任务，改由正常优先级从完整待执行队列开头选择。

网页“强制从头恢复”仅跳过无法识别页面时的会话检查，仍然从任务开头运行，不能恢复中间动作。可调整：

```bash
python3.12 tracking_click.py --resume-hotkey '<ctrl>+<alt>+r'
python3.12 tracking_click.py --resume-delay-seconds 5
python3.12 tracking_click.py --no-human-input-pause
python3.12 tracking_click.py --session-check-seconds 10
```

`SessionWatchdog` 默认每 15 秒只读识别账号入口、登录表单、游戏主页、服务器下载提示和异地登录提示。运行中命中这些页面会立即请求暂停并保存任务上下文。登录恢复是按当前页面推进的状态机，因此在公告页或下载确认页中途重启也不需要从账号入口重放。

进程在暂停时退出不会丢失旧断点。下次启动继续保持暂停；安全恢复后，旧任务若仍需处理就整条重跑，已记录锚点且仍在冷却的资源交互会跳过。

### 12.3 暂停时间与刷新时间

程序区分四种时间：

| 时间 | 暂停时如何处理 |
|---|---|
| 游戏资源刷新时间 | 按真实墙钟继续，不因用户占用桌面而延后 |
| 普通动作 delay | 暂停期间冻结；接受恢复后随整条路线重新计时 |
| `wait_until_scheduled` | 目标仍是固定墙钟；暂停期间到期则恢复后直接通过时间门 |
| 移动/提前量学习 | 扣除人工暂停和提前到达后的空等，不污染移速样本 |

所以暂停发生在宰杀之后时，`next_due` 仍从真实宰杀点击计算；暂停发生在宰杀之前时，程序不会把暂停数小时误学成旅行变慢。

### 12.4 资源类型与精确锚点

| 类型 | 任务 | 周期 | 起算动作 |
|---|---|---:|---|
| 猪圈/牛棚 | `pig1`、`pig2`、`dali_pig`、`dali_cow` | 60 分钟 | 宰杀确认点击 |
| 各地图牛 | `bear2/3/4/5/6/8/9/10/11/12/13/15` | 60 分钟 | 姑苏、杭州、泉州、洛阳、南阳渡、落霞镇、峨眉山、明月峰、龙泉镇、双王镇、华山、凤鸣集各自的宰杀确认点击 |
| 长白山熊与牛 | `bear1` | 60 分钟 | `长白熊` 移动点击记录熊；路线末尾宰杀确认记录长白山牛 |
| 野熊 | `bear_tianshan` 与 `bear1:bear` | 60 分钟 | 天山 `天山2`、长白山 `长白熊` 的自动战斗进入点击 |
| 塞北/天山/落日等牧场 | `bear7`、`bear14`、`cow1`、`cow2` | 180 分钟 | 每只牛羊的宰杀确认点击 |
| 水果 | `xigua`、`xiangjiao`、`shanzha`、`pingguo`、`changbaipingguo` | 西瓜 120 分钟，其余 300 分钟 | 每个采集交互的第一个确认点击 |
| 河虾/井水/莲藕/酸菜 | `hexia1/2`、`jianshui`、`lianou`、`suancai` | 保留原 360/300/1440 分钟 | 每个采集交互的第一个确认点击 |
| 家宅 | `jiazhai` | 原配置 | 尚无可靠资源动作，保守使用任务完成时间 |

旧 tuple 路线不再全部用路线结束时间。`resource_catalog.py` 识别 `边栏1/2 -> 空白 -> 确认` 的首个确认；长白山熊和天山熊使用命名移动点。`bear1` 会同时生成 `bear1:bear` 与 `bear1:cow`，其余旧 `bear*` 地图牛路线各生成一个牛点。连续数量或结果弹窗不会重复生成锚点。

多资源路线在 `resource_points` 下分别保存每个点，并学习：

- `anchor_offset_seconds`：从任务开头到该点真实刷新点击的活动耗时 EWMA；
- `anchor_offset_samples`：有效样本数；
- `segment_seconds`：上一资源锚点到当前锚点的活动耗时 EWMA。

样本会扣除人工暂停和资源闸门等待。调度器用各点 `next_due - anchor_offset_seconds` 中最早的时间作为出发窗口。到达交互前若该点将在默认 300 秒内刷新，就在 `边栏` 前等到精确时刻；更晚的点本轮跳过，避免为了一个长冷却点卡住整条队列。可用 `--resource-gate-max-wait-seconds` 调整。

`bear7`、`cow1`、`bear14` 在塞北、祁连和天山宰杀前有两层功德保护：先要求 `sleep1` 的“休息中”遮罩结束并识别蓝色 `子`，随后在每只牛羊的交互入口再次识别 `子`。任何一次读不到或已到白天都直接失败，不派发宰杀点击。2026-08-17 的隔离实测从白天出发，木床休息后 `子` 模板相似度为 `0.974`。

多点路线每个成功锚点都会立刻写本地状态和台账；除最后一点外，还执行一次游戏内 `save`，最后一点继续使用任务尾部原有 `save`。可用 `--no-intermediate-saves` 禁用游戏内中间保存。

若中途失败：

- 一个点都没触发：`failed`，默认 10 分钟后重试；
- 只触发部分点：`partial_failed`，已触发点保留自己的长周期；短期重走路线时会跳过仍在冷却的牲畜/野熊交互四步，只点击当前可用点；
- 失败退避单独保存为 `retry_not_before`；即使某个逐点刷新时间已经到期，或进程在失败后重启，也必须先等到该重试时间，避免立即失败循环占满队列；
- 全部资源动作已触发但保存/OCR 清理失败：`post_anchor_failed`，不短期重复宰杀。

### 12.5 Web 资源监控

`tracking_click.py` 默认同时提供：

```text
http://127.0.0.1:8765
```

页面包含任务/资源点状态、断点、操作日志、采集台账、估算汇总和暂停/恢复/停止控制。精确点击会立即追加到 `resource_history.jsonl`；数量代表采集事件估算，网页手工调整会标记为非估算记录。

所有 POST 接口都要求 `X-Yanyu-Request: dashboard` 请求头，页面会自动添加；局域网认证模式还必须通过 HTTP Basic 验证。

只查看文件而不打开游戏：

```bash
python3.12 monitor_server.py --host 127.0.0.1 --port 8765
```

独立模式不附着调度器，控制按钮不可用。绑定本机回环地址时仍可手工调整台账；未认证地绑定局域网地址时，所有 POST 都会被拒绝，只允许查看。

### 12.6 带认证的局域网控制

直接在局域网开放调度器控制时，程序强制要求随机密码和 TLS，缺少任意一项都会拒绝启动控制服务。首次在游戏 Mac 上创建私密文件：

```bash
CONTROL_DIR="$HOME/.config/yanyu-daemon"
mkdir -p "$CONTROL_DIR" && chmod 700 "$CONTROL_DIR"
umask 077
python3.12 -c 'import secrets; print(secrets.token_urlsafe(32))' > "$CONTROL_DIR/web-password"
openssl req -x509 -newkey rsa:2048 -sha256 -days 825 -nodes \
  -keyout "$CONTROL_DIR/web-key.pem" \
  -out "$CONTROL_DIR/web-cert.pem" \
  -subj '/CN=192.168.1.3' \
  -addext 'subjectAltName=IP:192.168.1.3,IP:127.0.0.1,DNS:localhost'
chmod 600 "$CONTROL_DIR/web-password" "$CONTROL_DIR/web-key.pem"
```

启动参数：

```bash
python3.12 tracking_click.py \
  --web-host 0.0.0.0 --web-port 8765 \
  --web-auth-user yanyu \
  --web-auth-token-file "$HOME/.config/yanyu-daemon/web-password" \
  --web-tls-cert-file "$HOME/.config/yanyu-daemon/web-cert.pem" \
  --web-tls-key-file "$HOME/.config/yanyu-daemon/web-key.pem"
```

另一台局域网设备访问 `https://192.168.1.3:8765`，用户名为 `yanyu`，密码保存在 `~/.config/yanyu-daemon/web-password`。首次访问需核对并接受自签名证书；其 SHA-256 指纹可用 `openssl x509 -in ~/.config/yanyu-daemon/web-cert.pem -noout -fingerprint -sha256` 查看。“安全从头恢复”可从已识别的登录页自动确认服务器最新存档；“强制从头恢复”只跳过会话识别，不会续跑旧动作。`8766` 保持只读，两个端口都不得映射到公网。

## 13. 游戏更新后的修复流程

1. 暂停坏任务，但保持其他任务运行：

```bash
python3.12 tracking_click.py --skip-task dali_pig
```

2. 查看 `scheduler_events.jsonl` 和 `scheduler_captures/*_failure_*.png`。智能错误包含动作序号、类型和标签。
3. 读取当前状态：

```bash
python3.12 procedure_runner.py --read-state
```

4. 使用相同 `--name` 重新录制并 `--install`。raw 文件带时间戳，旧证据不会丢；安装文件会替换。
5. 分析并用独立单元测试验证结构：

```bash
python3.12 recording_analyzer.py procedures/dali_pig.json
python3.12 procedure_runner.py dali_pig --dry-run --startup-delay 0
python3.12 -m unittest discover -s tests -v
```

6. 在资源可用时单独端到端运行一次，再恢复 7x24 调度。

同名智能流程不会要求修改 `tracking_click.py`，原任务的持久化刷新时间也会保留。

## 14. 数据量化与学习

### 14.1 录制分析

```bash
python3.12 recording_analyzer.py recordings/my_route_*.jsonl
python3.12 recording_analyzer.py procedures/my_route.json
```

报告包括：

- 点击/拖动数量；
- 总等待和鼠标按住时长；
- 地图校验点和实测稳定时间；
- OCR 文字定位覆盖；
- F5 快速组数量、平均和最大点击间隔；
- 刷新锚点位置。

`F6/F7` 会作为追加式 annotation 写入 raw JSONL；分析器按动作序号回放 annotation，并保证后一次 `F7` 取代前一次唯一锚点。这样 procedure 安装文件与原始训练证据保持一致。

### 14.2 运行时耗时

每次成功到达 `expect` 后，耗时写入 `runtime_profiles.json`：

```text
last_seconds
ewma_seconds
samples
updated_at
```

它用于设置合理 timeout 和评估移动速度，不会用均值代替实时坐标确认。

样本按动作类型、标签、点位和期望状态生成的稳定指纹保存，不再按数组序号关联。插入前置检查或公共片段不会让旧耗时错配；点位或目标状态真正改变时会生成新指纹并重新学习。必要时可显式设置 `timing_id` 区分两个语义不同但内容完全相同的动作。

### 14.3 屏幕变化

旧路线可开启逐动作截图，再用 `pattern_learner.py` 比较像素变化。低变化动作可能无效或重复，高变化动作通常是加载、对话或战斗。OCR 坐标/文字仍应作为主要成功依据。

## 15. 验证命令

```bash
python3.12 -m py_compile automation.py smart_automation.py game_session.py tracking_click.py runtime_control.py resource_catalog.py monitor_server.py
python3.12 -m unittest discover -s tests -v
python3.12 procedure_runner.py --list
python3.12 procedure_runner.py dali_cow --dry-run --startup-delay 0
python3.12 procedure_runner.py dali_pig --dry-run --startup-delay 0
python3.12 recording_analyzer.py procedures/dali_cow.json
python3.12 recording_analyzer.py procedures/dali_pig.json
python3.12 tracking_click.py --list
```

## 16. 安全边界

- 新轻功路线先录制成功样本，不凭截图猜多段落点。
- 多段跳期间不要切换窗口、截图或运行其他高负载任务。
- 不对普通移动启用盲目重试。
- `refresh_anchor` 之后的失败不能触发短周期重杀。
- OCR 失败时，关键资源动作使用 `target_required: true`，不盲点 fallback。
- 新路线先 dry-run，再逐段状态验证，最后才加入调度。
- 游戏地图、UI、角色移速或客户端窗口比例更新后，已有数据只是历史样本，必须重新验证受影响片段。
